export const getQuestionsData = (state) => state.questions.questionsData;

export const getQuestionsLoading = (state) => state.questions.questionsLoading;

export const getQuestionsFailed = (state) =>
  state.questions.questionsLoadFailed;
